diff --git a/PKGBUILD b/PKGBUILD
index 2c399f2..b597276 100644
--- a/PKGBUILD
+++ b/PKGBUILD
@@ -23,6 +23,8 @@ prepare() {
 
   # download dependencies
   export GOPATH="${srcdir}"
+  # XXX: changing libc versions/presets can cause linking to fail due to the compilation cache.
+  go clean -cache
   go mod download
 }
 
